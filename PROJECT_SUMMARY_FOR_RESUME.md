# 英国人口老龄化趋势预测与可视化平台

- **技术栈**: TypeScript, React 18, Vite, FastAPI, Python, Recharts, Tailwind CSS, Prophet, ARIMA, scikit-learn, Pandas

- **项目简介**: 基于英国国家统计局（ONS）官方人口数据，独立设计并开发了一套全栈数据可视化 Web 应用。后端使用 FastAPI 将 Prophet、ARIMA 时间序列预测模型与 KMeans 聚类分析服务化，对英格兰、苏格兰、威尔士三地区 65+ 老龄化比例进行 2020–2150 年长周期预测；前端基于 React 18 构建三大交互页面，支持地区、模型多维度切换与实时图表渲染；实现从数据清洗、模型训练、API 服务化到前端可视化的完整全栈闭环。

- **工作内容与成果**:

  - **前端开发**: 基于 React 18 + Vite 5 从零搭建 SPA，Tailwind CSS 完成样式工程化，React Router v6 实现多页面路由；抽象 5+ 个可复用组件，`useState` + `useEffect` 统一管理异步数据流与边界错误；Dashboard 通过 `Promise.all` 并行请求减少等待，Forecast 按「地区 × 模型」精准触发对应接口，Cluster 支持聚类数实时切换并无缝刷新图表；封装统一 Axios 请求层，Vite 反向代理解决开发跨域。

  - **数据可视化**: 运用 Recharts 渲染时序数据；手写 pivot 函数将后端扁平数据转换为多系列格式驱动多折线并排；Forecast 页动态标注历史/预测分界线，历史段与预测段双轨对比，并同步展示 Prophet vs ARIMA 的 MAE / RMSE / MAPE 评估表格；Cluster 页按聚类结果动态着色，附各地区 65+ 占比变化摘要卡片；全部折线禁用逐点渲染（`dot={false}`），优化 130 年长序列的图表性能。

  - **后端 API 设计**: 基于 FastAPI 设计路由层与业务层分离的双层架构，规范 7 个 RESTful 端点（历史数据、总览统计、Prophet/ARIMA 预测、模型评估指标、聚类分析）；配置 CORS 中间件；定位并解决 Swagger UI 外部 CDN 在受限网络下不可用的问题，将静态资源本地化挂载，消除外部依赖风险；API 响应时间 < 100ms（读取预生成 CSV，无在线推理延迟）。

  - **预测建模**: 使用 Prophet（逻辑增长模型，配置 cap/floor 约束与 changepoint 弹性调优、滚动平均平滑）和 ARIMA（pmdarima 自动定阶）对英格兰、苏格兰、威尔士三地区 65+ 老龄化比例进行 2020–2150 年长周期预测，输出 MAE / RMSE / MAPE 三项指标对比两模型效果。

  - **数据处理与聚类**: 使用 Pandas / NumPy 清洗来自英国国家统计局（ONS）的原始人口数据，完成格式标准化与特征构造；设计 8 步模块化 Pipeline（数据清洗 → 合并 → 趋势分析 → Prophet 建模 → ARIMA 建模 → 多地区对比 → 聚类分析），各步骤独立可重跑；对各地区时间序列经 StandardScaler 归一化后输入 KMeans（支持 2~4 聚类动态切换），按趋势轨迹相似度将地区分组。



---

## 🎤 面试口述稿

### 面试题：介绍一下你的项目

**30 秒版（电梯回答）**
> 我做的是一个英国人口老龄化趋势预测与可视化平台。项目是全栈的，我独立完成了从数据清洗、建模、后端接口到前端展示的完整链路。后端用 FastAPI 把 Prophet、ARIMA 和 KMeans 封装成 API，前端用 React 做交互式图表，用户可以按地区和模型实时切换，查看 2020-2150 年老龄化趋势和评估指标。

**90 秒版（面试展开）**
> 这个项目的核心目标是把英国国家统计局的原始人口数据，变成一个可交互、可解释的预测分析系统。
>
> 第一部分是数据和模型层。我用 Pandas 做数据清洗、标准化和多源融合，然后分别用 Prophet 和 ARIMA 做时间序列预测，并用 MAE、RMSE、MAPE 做模型对比。除此之外，我还用了 KMeans 聚类，识别不同地区老龄化轨迹的相似性。
>
> 第二部分是后端服务层。我用 FastAPI 设计了 7 个 REST 接口，覆盖历史数据查询、模型预测、指标评估和聚类结果。代码结构采用 routers 和 services 分层，便于维护和扩展。因为采用预生成结果，接口响应基本在 100ms 以内。
>
> 第三部分是前端交互层。我用 React 搭了 Dashboard、Forecast、Cluster 三个页面。用户切换地区或模型时，图表会实时刷新。这里有个关键技术点是后端返回扁平数据，前端图表需要多序列结构，我写了数据重组逻辑把它转换成可渲染格式，保证了可视化效果和长时间序列下的性能。

**一句话总结（收尾）**
> 这个项目让我不仅能做数据分析和建模，还能把模型真正服务化并交付给用户使用，形成端到端的产品能力。

### 追问准备：你遇到过什么技术难点？

> 我遇到过两个比较典型的技术难点。
>
> 第一，前后端图表数据结构不一致。后端返回扁平记录，前端多折线图需要按年份对齐的多序列结构。我增加了一层数据转换逻辑，按年份聚合并按地区补齐字段，最终实现了模型和地区切换时的稳定渲染，也避免了长序列下的卡顿。
>
> 第二，API 文档在受限网络下不可用。FastAPI 的 Swagger 默认依赖外部 CDN，网络受限时会白屏。我把 Swagger 静态资源改为本地托管，去掉外部依赖后，内网和受限环境都可以稳定访问。
>
> 这两个问题让我形成了一个排障习惯：先判断问题属于数据层、接口层还是展示层，再做最小改动验证，整体效率会更高。

### 追问准备：Prophet 和 ARIMA 你怎么选的？

> 我不是先拍脑袋选模型，而是先做并行验证。我把同一地区的数据分别跑 Prophet 和 ARIMA，用 MAE、RMSE、MAPE 三个指标做对比，再结合数据特征和业务场景选择。
>
> 如果数据存在明显趋势变化和拐点，我会优先 Prophet；如果序列更平稳、线性结构更明显，我会优先 ARIMA。在这个项目里我保留了双模型结果，让用户可以直接对比，而不是强制只看一个模型。

### 追问准备：介绍你的项目架构？

> 我的项目是前后端分离的全栈架构，整体分四层。
>
> 第一层是数据层，负责 ONS 原始数据清洗、标准化和特征构造，输出稳定可复用的数据集。
>
> 第二层是算法层，核心包括 Prophet 和 ARIMA 的时间序列预测、MAE/RMSE/MAPE 指标评估，以及 KMeans 聚类分析。
>
> 第三层是服务层，我用 FastAPI 把数据查询、预测结果、评估指标和聚类结果封装为 REST API，采用 routers + services 分层，提升可维护性和可扩展性。
>
> 第四层是展示层，前端用 React + Recharts 构建 Dashboard、Forecast、Cluster 三个页面，通过 Axios 调用后端接口，实现地区、模型、聚类数的动态切换和实时渲染。
>
> 一句话总结：这是一个“数据处理 + 模型分析 + API 服务化 + 前端交互展示”的端到端架构。

### 追问准备：这个项目和纯前端项目有什么不同？

> 最大区别是我负责的是完整链路，而不是只做页面渲染。纯前端项目里接口通常是既定的，这个项目里接口是我自己设计的；纯前端主要处理展示逻辑，这个项目还要处理数据清洗、模型训练、评估和服务化。
>
> 调试维度也更复杂，需要判断问题在数据层、模型层、接口层还是前端层。这个项目让我从“会做页面”变成“能交付端到端的数据产品”。
